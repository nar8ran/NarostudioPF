# 編集ガイド — 自分でサイトを書き換えるための説明書

素の HTML / CSS / JS で作った 4 ページのポートフォリオサイトです。
**ビルド不要**。ファイルを直接編集して、`index.html` をブラウザで開けば確認できます。

---

## 1. ファイル構成と役割

```
my-portfolio/
├─ index.html    … トップ（動画エリア＋右サイドの縦ナビ）
├─ about.html    … About（プロフィール・使用ソフト・資格・pickup）
├─ works.html    … Works（カテゴリ絞り込み・三列グリッド）
├─ contact.html  … Contact（お問い合わせフォーム＋横長イラスト枠）
├─ style.css     … 見た目（色・レイアウト）※全ページ共通で読み込み
├─ script.js     … 動き（動画切替・タブ絞り込み・フォーム）※全ページ共通
├─ images/       … 画像（png / jpg）
├─ movie/        … 動画（mp4）
├─ audio/        … 音楽（mp3）
└─ source/       … Illustrator 等の元データ（Webには表示しない）
```

- **CSS と JS は 4 ページ共通**：色や動きを変えると全ページに反映されます。
- **HTML は 1 ページ 1 ファイル**：中身はページごとに直接書きます。

---

## 2. まず知っておく「仕組み」3 つ

### (1) ヘッダー / フッターは各ページに“コピペで直書き”されている
このサイトは共通パーツを自動で読み込む仕組み（テンプレート）を**使っていません**。
つまり **上のナビ・お名前・SNS を変えるときは 4 ファイルすべてを直す**必要があります。

> 💡 コツ：エディタの「フォルダ全体を検索して置換」で `Your Name` などを一括置換すると楽です。

### (2) クラス名はブロック単位で付いている（BEM 風）
`site-header`（ブロック）＝ `site-header__inner`（要素）のように、
`ブロック__要素` の形。CSS を探すときの目印になります。

### (3) `script.js` がやっていること
- **トップ**：`#hero` 内の `<video class="hero__slide">` をランダムに切り替え
- **Works**：`.tab`（`data-filter`）を押すと、`.card`（`data-category`）を絞り込み
- **Contact**：必須項目（お名前・メール・内容）をチェックして完了メッセージを表示

カテゴリ名などは JS に直接書いていないので、**HTML 側を増やすだけで動きます**（後述）。

---

## 3. ページごとの中身

### index.html（トップ）
```html
<section class="hero" id="hero">
  <video class="hero__slide is-active" src="movie/L2DIdol.mp4" autoplay muted playsinline preload="auto"></video>
  <video class="hero__slide"           src="movie/L2Dkemo.mp4" muted playsinline preload="metadata"></video>
</section>

<aside class="home-side">
  <div class="home-brand"> … 名前・肩書き … </div>
  <nav class="home-nav"> … About / Works / Contact … </nav>
  <div class="home-links"> … Commission / Link / X … </div>
</aside>
```
- `<video class="hero__slide">` を**増やせば**、その分ランダム切替の候補が増えます。
- 最初の 1 本にだけ `is-active` と `autoplay` を付けます。

### about.html
- `.page-head` … 大見出し「About」＋日英サブコピー
- `.profile` … プロフィール写真（プレースホルダー）＋自己紹介＋使用ソフト＋取得資格
- `pickup` セクション … `.card` を 6 枚

### works.html
```html
<div class="tabs" id="tabs">
  <button class="tab active" data-filter="all">All</button>
  <button class="tab" data-filter="original">Original</button>
  <button class="tab" data-filter="vtuber">VTuber</button>
  <button class="tab" data-filter="other">Other Works</button>
</div>

<div class="grid-3" id="works-grid">
  <article class="card" data-category="original"> … </article>
  …
</div>
```

### contact.html
- `<form id="contact-form">` … 各入力欄
- `script.js` が入力チェック＆完了メッセージ表示（実送信は Formspree 等で。§7 参照）

---

## 4. 自分で編集するレシピ集（コピペ可）

### A. 名前・肩書き・SNS を変える
- **名前**：各 HTML の `Your Name` を検索 → 置換（4 ファイル）
- **肩書き**：`Creator / Illustrator`
- **SNS**：`href="#"` を実 URL に。使わないリンクは行ごと削除

### B. 画像を追加・差し替える
1. 画像を `images/` に入れる（例：`chara01.png`）
2. プレースホルダー（下の `ph` の部分）を `<img>` に置き換える

```html
<!-- 置き換え前（プレースホルダー） -->
<div class="ph ph--sage"><span class="ph__label">Sample</span></div>

<!-- 置き換え後 -->
<img src="images/chara01.png" alt="オリジナルキャラクター" />
```
> `alt=""` には作品の説明を入れると SEO・アクセシビリティに◎

### C. Works に「画像」の作品を追加する
`works.html` の `<article class="card">…</article>` を 1 個コピーして中身を差し替え：

```html
<article class="card" data-category="original">
  <div class="card__media">
    <img src="images/新しい作品.png" alt="作品名" />
  </div>
  <div class="card__meta">
    <p class="card__title">作品名</p>
    <span class="card__cat">Original</span>
  </div>
</article>
```
- `data-category` は `original` / `vtuber` / `other` のいずれか
- これだけでタブの絞り込みにも自動対応します

### D. Works に「動画（mp4）」を入れる（クリックで再生）
```html
<article class="card" data-category="vtuber">
  <div class="card__media">
    <video src="movie/L2Dsummer.mp4" controls preload="metadata"></video>
  </div>
  <div class="card__meta">
    <p class="card__title">Live2D Summer</p>
    <span class="card__cat">VTuber</span>
  </div>
</article>
```
- `controls` で再生ボタンが出ます（重い動画を全部自動再生しないための形）

### E. Works に「YouTube」を埋め込む
```html
<article class="card" data-category="other">
  <div class="card__media">
    <iframe
      src="https://www.youtube.com/embed/動画ID"
      title="作品名"
      allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
      allowfullscreen
      loading="lazy"></iframe>
  </div>
  <div class="card__meta">
    <p class="card__title">作品名</p>
    <span class="card__cat">Other</span>
  </div>
</article>
```
- **動画ID** は YouTube の URL（`https://www.youtube.com/watch?v=XXXXXXXX`）の `XXXXXXXX` の部分

### F. トップの動画を変える / 増やす
`index.html` の `#hero` の中の `<video>` を編集：
```html
<video class="hero__slide" src="movie/好きな動画.mp4" muted playsinline preload="metadata"></video>
```
- `src` を変えるだけで差し替え、`<video>` を足すだけで切替候補が増えます
- **音声は必ずミュート**（音付き自動再生はブラウザが禁止しているため）

### G. カテゴリ（タブ）を増やす・減らす
`works.html` の `.tabs` にボタンを足し、カードの `data-category` を合わせるだけ：
```html
<button class="tab" data-filter="music">Music</button>
<!-- 対応するカード -->
<article class="card" data-category="music"> … </article>
```
JS はカテゴリ名を固定していないので、これで動きます。

### H. 色を変える
`style.css` の先頭 `:root` の変数を書き換える（全ページに反映）：
```css
:root {
  --cream: #faf7f2;   /* 背景 */
  --ink:   #40382f;   /* 文字 */
  --sage:  #a6ccb4;   /* メインの緑 */
  --blush: #f3c6c9;   /* 差し色ピンク */
  --sage-deep: #5f9d7c;  /* リンク・ボタン */
  …
}
```

---

## 5. GitHub Pages で公開する

1. GitHub で新規リポジトリを作成（例：`portfolio`、Public）
2. ローカルから push
   ```bash
   git init
   git add .
   git commit -m "portfolio site"
   git branch -M main
   git remote add origin https://github.com/ユーザー名/portfolio.git
   git push -u origin main
   ```
3. リポジトリの **Settings → Pages → Build and deployment**
   - Source: **Deploy from a branch**
   - Branch: **main** / **/(root)** → Save
4. 数分後、`https://ユーザー名.github.io/portfolio/` で公開されます
5. 独自ドメインは同じ Pages 設定の **Custom domain** から追加できます

> このサイトはリンクをすべて相対パス（`about.html`・`images/…`）で書いているので、
> サブパス（`/portfolio/`）配信でもそのまま動きます。

### ⚠️ 動画の容量に注意
- GitHub は **1 ファイル 100MB まで**、リポジトリは **1GB 以下推奨**。
- 今の `movie/` は合計 234MB。ファイル単位は大丈夫ですが、**公開前に圧縮**するか、
  重い動画は **YouTube に載せて §4-E で埋め込む**のがおすすめです。
- 動画をリポジトリに含めたくない場合は `.gitignore` に `movie/` を追記して除外できます。
  （その場合、動画は YouTube か別ホストに置く）

---

## 6. お問い合わせフォームを実際に動かす（Formspree）

今は入力チェックと完了メッセージのみで、**メールは送信されません**。
実際に受け取るには、無料枠のある [Formspree](https://formspree.io/) が手軽です。

`contact.html` の `<form>` を次のように変更：
```html
<form class="form" id="contact-form" action="https://formspree.io/f/あなたのID" method="POST">
```
`action` を設定すると、`script.js` は入力チェック後に **そのまま Formspree へ送信**します
（`action` が無いときだけ、その場で完了メッセージを出す作りになっています）。

---

## 7. 困ったとき

- **プレビュー**：`index.html` をダブルクリックでブラウザ表示。フォントは Google Fonts を
  読み込むためネット接続が必要（オフラインでも代替フォントで表示されます）。
- **動画が出ない**：`src` のファイル名（大文字小文字・拡張子）が `movie/` の実ファイルと
  一致しているか確認。
- **レイアウトが崩れた**：タグの閉じ忘れ（`</div>` など）が多いです。コピー元のカードと
  見比べてください。
